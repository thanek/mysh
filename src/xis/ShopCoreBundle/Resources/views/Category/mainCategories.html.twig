<ul>
    {% for category in categories %}
        <li>
            <a href="{{ path('category',{'slug':category.slug,'id': category.id}) }}">{{ category.name }}</a>
        </li>
    {% endfor %}
</ul>